# HTTP(hyper text transfer protocol) Module
It is built in Node.js Module which allows developers to create web server and web client without installing any external package.

One od the most important module in Node.js, originally designed for building scalabel network applivation and web servers.

1. create web servers 
2. hadle client request
3. Build REST APIs
4. Non-Blocking I/O
5. Foundation of Express.js